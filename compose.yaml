services:
  circuitjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: labs-electronic-circuitjs
    ports:
      - "${ELECTRONICS_PORT:-8082}:80"
    restart: unless-stopped
